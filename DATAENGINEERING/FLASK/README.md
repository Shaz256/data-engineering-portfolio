# Flask Projects

This folder contains Flask-based backend projects.

- product_api → REST API with Swagger and PostgreSQL
To run just do python run.py it will redirect to http://127.0.0.1:5000/ and to add, delete, update and view you can use swaggerlink is http://127.0.0.1:5000/apidocs/